# NovaBot - Production runtime dependencies only
# Keep lean for Coolify builds (small LXC RAM/swap). Transitive deps
# (httpx, requests, dateutil, …) come from openai / hyperliquid-sdk / pandas.

# Core Framework
fastapi==0.115.5
uvicorn==0.32.1
pydantic==2.10.3

# Data Processing
pandas>=2.3.2
numpy>=2.2.6

# WebSocket (bot feed; HL SDK uses websocket-client separately)
websockets>=10.0,<14.0

# AI/LLM
openai==1.57.4

# Discord Integration
discord-webhook==1.3.1

# Environment Variables
python-dotenv==1.0.1

# Hyperliquid (eth-account pulled transitively; pin for HL 0.24 compat)
eth-account>=0.10.0,<0.14.0
hyperliquid-python-sdk==0.24.0
