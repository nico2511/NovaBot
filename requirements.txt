# NovaBot - Trading Bot Requirements
# Python 3.10+
#
# Default install for local/dev: production + test/lint tools.
# Docker production image uses requirements-prod.txt only (avoids OOM from mypy/llvmlite).

-r requirements-dev.txt
